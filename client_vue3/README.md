# MCP Inspector Client (Vue 3)

이 디렉터리는 기존 React 기반 클라이언트(`client/`)를 Vue 3로 간단하게 옮긴 예제입니다.
모든 기능을 완전히 구현하지는 않았지만 기본적인 STDIO 호출과 MCP 세션 연결을 테스트할 수 있습니다.

## 설치

```bash
cd client_vue3
npm install
```

## 개발 서버 실행

```bash
npm run dev
```

브라우저에서 `http://localhost:6275` 로 접속하면 간단한 UI를 볼 수 있습니다.
